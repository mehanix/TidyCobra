'''
    Wrapper class that stores associations between paths and the file extensions to be moved to their respective paths.
'''

rules = dict() # rule format: "absolute_path_to_folder" : ["extensions","array"]
